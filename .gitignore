# =============================================================
# Rust / Cargo
# =============================================================
/target/
**/target/
**/*.rs.bk
Cargo.lock.bak

# =============================================================
# Internal / maintainer-only docs (protocol spec, playbooks).
# Kept locally, never pushed. Public docs are at repo root.
# =============================================================
/internal/

# =============================================================
# Runtime state (identity, ACME cache, admin_token, tunnel data)
# =============================================================
/var/
/data/
authorized_keys
peers.txt
identity
admin_token.hash
*.pem
*.key

# =============================================================
# Soak / load test output
# =============================================================
/krot-soak-out/
/scripts/soak/env

# =============================================================
# Editor / IDE / OS
# =============================================================
.DS_Store
Thumbs.db
.idea/
.vscode/
.claude/
*.swp
*.swo
*~

# =============================================================
# Deploy tooling: contains server address + credentials. NEVER push.
# =============================================================
/scripts/

# =============================================================
# Vendored crates + tarball for offline docker builds (regenerate:
# cargo vendor vendor && tar -cf vendor.tar vendor). Too big for git.
# =============================================================
/vendor/
/vendor.tar
